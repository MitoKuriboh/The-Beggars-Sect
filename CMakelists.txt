cmake_minimum_required(VERSION 3.16)
project(TheBeggarsPath C)

set(CMAKE_C_STANDARD 17)
set(CMAKE_C_STANDARD_REQUIRED ON)

# Warnings
if (MSVC)
  add_compile_options(/W4 /permissive-)
  add_definitions(-D_CRT_SECURE_NO_WARNINGS)
else()
  add_compile_options(-Wall -Wextra -Wpedantic)
endif()

# Grab all .c files in src (Legacy/ is ignored)
file(GLOB SRC_FILES CONFIGURE_DEPENDS "${CMAKE_SOURCE_DIR}/src/*.c")

# Build the game
add_executable(beggars ${SRC_FILES})

# If later you move headers to /include, uncomment:
# target_include_directories(beggars PRIVATE ${CMAKE_SOURCE_DIR}/include)
